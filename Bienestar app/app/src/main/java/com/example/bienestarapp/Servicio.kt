package com.example.bienestarapp

data class Servicio(
    val id: Long,
    val nombre: String,
    val descripcion: String
    // Asegúrate de que los nombres de las variables coincidan con los campos
    // de tu entidad `Servicio` en el backend.
)
