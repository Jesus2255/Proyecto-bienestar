# =============================================================================
# ARCHIVO DE EJEMPLO DE VARIABLES DE ENTORNO PARA SUPABASE
# =============================================================================
# 
# ⚠️ IMPORTANTE - SEGURIDAD:
# - NO commitear este archivo con valores reales
# - Copiar este archivo como .env y llenar con valores reales
# - Añadir .env al .gitignore
# - Usar GitHub Secrets para CI/CD
# - NUNCA exponer SERVICE_ROLE_KEY en frontend (Android/HTML)
#
# =============================================================================

# -----------------------------------------------------------------------------
# SUPABASE API KEYS
# -----------------------------------------------------------------------------
# Obtener desde: Supabase Dashboard > Settings > API
# URL del proyecto Supabase (ej: https://xxxxx.supabase.co)
SUPABASE_URL=

# Anon/Public Key - SEGURA para usar en frontend (Android, HTML5)
# Requiere Row Level Security (RLS) habilitado en las tablas
SUPABASE_ANON_KEY=

# Service Role Key - ⚠️ SOLO BACKEND - Bypasses RLS
# NUNCA usar en aplicaciones cliente (Android/HTML)
SUPABASE_SERVICE_ROLE_KEY=

# -----------------------------------------------------------------------------
# DATABASE CONNECTION - Opción 1: DATABASE_URL (formato Heroku/Supabase)
# -----------------------------------------------------------------------------
# Obtener desde: Supabase Dashboard > Settings > Database > Connection string > URI
# Formato: postgres://postgres:PASSWORD@db.xxxxx.supabase.co:5432/postgres
#
# Puerto 5432: Direct connection / Transaction Pooler
# Puerto 6543: Session Pooler (recomendado para apps con muchas conexiones)
#
DATABASE_URL=postgres://postgres:YOUR_PASSWORD@db.xxxxx.supabase.co:5432/postgres

# -----------------------------------------------------------------------------
# DATABASE CONNECTION - Opción 2: Variables JDBC específicas (prioridad)
# -----------------------------------------------------------------------------
# Estas variables tienen prioridad sobre DATABASE_URL
# Útil para mantener compatibilidad con application-prod.properties
#
JDBC_DATABASE_URL=jdbc:postgresql://db.xxxxx.supabase.co:5432/postgres
JDBC_DATABASE_USERNAME=postgres
JDBC_DATABASE_PASSWORD=YOUR_PASSWORD

# -----------------------------------------------------------------------------
# NOTAS IMPORTANTES:
# -----------------------------------------------------------------------------
#
# 1. OBTENER CREDENCIALES:
#    - Dashboard: https://supabase.com/dashboard/project/_/settings/api
#    - Database password: Settings > Database > Database password
#
# 2. CONNECTION POOLING:
#    - Puerto 5432: Direct connection (usar con HikariCP)
#    - Puerto 6543: Session Pooler (mejor para apps con pool propio)
#    - Ver documentación: https://supabase.com/docs/guides/database/connecting-to-postgres
#
# 3. IPv4 COMPATIBILITY:
#    - Si ves warning "Not IPv4 compatible" en dashboard
#    - Opciones: Usar Session Pooler (puerto 6543) o IPv4 add-on
#    - Alternativa: Usar Supabase REST API en vez de conexión directa
#
# 4. ROW LEVEL SECURITY (RLS):
#    - OBLIGATORIO cuando usas anon_key desde frontend
#    - Configurar políticas en: Dashboard > Authentication > Policies
#    - service_role_key bypasses RLS (solo backend)
#
# 5. DEPLOYMENT:
#    - GitHub Actions: Settings > Secrets > Actions
#    - Heroku: heroku config:set VARIABLE=valor
#    - Render: Dashboard > Environment
#    - Vercel: Settings > Environment Variables
#
# =============================================================================
